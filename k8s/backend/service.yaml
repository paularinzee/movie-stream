apiVersion: v1
kind: Service
metadata:
  name: movie-stream-api
  namespace: movie-stream
spec:
  selector:
    app: movie-stream-api
  ports:
    - port: 5000
      targetPort: 5000
  type: ClusterIP